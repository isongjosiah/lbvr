\section{Protocol Design}
\label{sec:protocol}

% TODO: P1.4 — ~1.5 pages (~1.4k words). Compress from
% docs/erasure-design.md and docs/provenance-spec.md. Must contain:
%
%   \subsection{Tiered placement}: hot/warm/cold mapping; placement
%     policy.
%
%   \subsection{Erasure coding (RS(2,3))}: ~0.4 pages. Compress from
%     erasure-design.md \S3-\S4. Why RS(2,3) and not higher parity.
%
%   \subsection{Cryptographic provenance}: ~0.4 pages. Compress from
%     provenance-spec.md \S2-\S3. PROV-JSON extension; BLS quorum;
%     JCS canonicalization; on-chain anchoring.
%
%   \subsection{PoR challenge/response/verdict}: contract API; nonce
%     binding; auto-migration after k consecutive failures.
%
%   \subsection{Quorum retrieval}: parallel-3 fetch + first-2 wins;
%     SLO budget; cross-tier reconstruction; circuit breaker.
%
%   \subsection{Registry schema}: BundleRecord shape; on-chain
%     numChunks binding; bundleId derivation.
